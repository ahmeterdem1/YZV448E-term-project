from torch.utils.data import DataLoader

def get_partitioned_dataset(*args, **kwargs) -> tuple:
    """
        Returns a tuple of (train_dataset, val_dataset, test_dataset) based on the provided arguments.
        Validation split may be omitted.
    """

    pass

